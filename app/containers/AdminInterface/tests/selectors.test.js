// import { selectAdminInterfaceDomain } from '../selectors';

describe('selectAdminInterfaceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
