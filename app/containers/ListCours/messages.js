/*
 * ListCours Messages
 *
 * This contains all the text for the ListCours container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListCours';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListCours container!',
  },
});
