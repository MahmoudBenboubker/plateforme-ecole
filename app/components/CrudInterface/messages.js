/*
 * CrudInterface Messages
 *
 * This contains all the text for the CrudInterface component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CrudInterface';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CrudInterface component!',
  },
});
