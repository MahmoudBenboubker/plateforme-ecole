/*
 * CoursInterface Messages
 *
 * This contains all the text for the CoursInterface container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CoursInterface';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoursInterface container!',
  },
});
