/*
 * Inscription Messages
 *
 * This contains all the text for the Inscription container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Inscription';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Inscription container!',
  },
});
