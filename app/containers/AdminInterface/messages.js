/*
 * AdminInterface Messages
 *
 * This contains all the text for the AdminInterface container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminInterface';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminInterface container!',
  },
});
