/*
 * CoursCard Messages
 *
 * This contains all the text for the CoursCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CoursCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoursCard component!',
  },
});
