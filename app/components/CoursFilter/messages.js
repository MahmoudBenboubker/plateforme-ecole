/*
 * CoursFilter Messages
 *
 * This contains all the text for the CoursFilter component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CoursFilter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoursFilter component!',
  },
});
