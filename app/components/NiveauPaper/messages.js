/*
 * NiveauPaper Messages
 *
 * This contains all the text for the NiveauPaper component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NiveauPaper';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NiveauPaper component!',
  },
});
